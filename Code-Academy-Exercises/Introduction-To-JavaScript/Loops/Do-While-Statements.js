let cupsOfSugarNeeded = 2;
let cupsAdded = 0;

do {
  cupsAdded = cupsAdded + 1;
} while (cupsAdded < cupsOfSugarNeeded);

console.log(cupsAdded);

// Make a piece of code to run at least once and then loop based on a specific condition after its initial run